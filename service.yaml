apiVersion: v1
kind: Service
metadata:
  labels:
    app: asetco-back-end
  name: asetco-back-end
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
  selector:
    app: asetco-back-end
  type: ClusterIP

